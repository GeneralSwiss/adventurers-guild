[package]
name = "guild-domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "The Adventurers' Guild contract, escrow, and settlement domain."

[dependencies]
thiserror.workspace = true
num-rational.workspace = true

[dev-dependencies]
proptest.workspace = true

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"
